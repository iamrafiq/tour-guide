NODE_ENV=development
PORT=3000
DATABASE=mongodb+srv://rafiq:<PASSWORD>@cluster0-eeev8.mongodb.net/tour-guide?retryWrites=true&w=majority
DATABASE_LOCAL=mongodb://localhost:27017/tours-test
DATABASE_PASSWORD_ATLAS=QMKFKwhPXhpb6ydM

#configuration file is perfect for storing JWT SECRET data
#Using the standard HSA 256 encryption for the signature, 
#the secret should at least be 32 characters long,
#but the longer the better.
JWT_SECRET = my-32-character-ultra-secure-and-ultra-long-secret
#after 90days JWT will no longer be valid,
#even the signuter is correct and everything is matched.
JWT_EXPIRES_IN=90